---

$id: workload

type: object

properties:
  scenario:
    $ref: scenario
    default: {}
  media:
    type: string
    format: path
  measurement:
    type: object
    default: {}
    properties:
      throughput:
        type: object
        default: {}
        properties:
          warm-up:
            type: integer
            default: 2
          sample-size:
            type: integer
            default: 100
          duration:
            type: integer
            default: 60
          select:
            type: string
            default: avg
            enum: [max, min, avg]
        required: [warm-up, sample-size, duration, select]
#      latency:
#        type: object
    
      density:
        type: object
        properties:
          multiprocess:
            type: boolean
            default: true
          fps:
            type: integer
            default: 30
          warm-up:
            type: integer
            default: 2
          sample-size:
            type: integer
            default: 100
          duration:
            type: integer
            default: 60
          minimum-range:
            type: array
            items:
              type: number
            minItems: 1
            maxItems: 2
          average-range:
            type: array
            items:
              type: number
            minItems: 1
            maxItems: 2
            default: [29]
          starting-streams:
            type: integer
          fixed-streams:
            type: integer
          max-iterations:
            type: integer
            default: -1
          min-streams:
            type: integer
            default: 1
          max-streams:
            type: integer
            default: 100
        required: [fps, max-streams, min-streams, warm-up, sample-size, duration, max-iterations]  
      
required: [scenario, measurement]
